# Send a email

Api for send email.

Add a .env file in proyect root

	SMTP_HOST=Host
	SMTP_PORT=Port
	SMTP_USER=User
	SMTP_PASS=Pass
	SMTP_TO=To
	CORS_ORIGIN=CO

Run the following commands to start the application
	-npm install
	-npm start
	


